When pushing to master, perform these steps

-update .env file
-sudo npm install && sudo npm install bcrypt
-add socket option and point it to .sock in db.js
-install mysql-server again if it didn't work
